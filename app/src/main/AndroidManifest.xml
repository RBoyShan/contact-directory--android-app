<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.contact_directory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contactdirectory">
        <activity android:name=".activity.RegistrationActivity"></activity>
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".activity.ContactDetailActivity" />
        <activity
            android:name=".activity.ContactDetailActivity"
            tools:ignore="DuplicateActivity" />
        <activity
            android:name=".activity.ContactDirectory"
            android:label="@string/title_activity_contact_directory"
            android:theme="@style/Theme.Contactdirectory.NoActionBar" />
        <activity android:name=".activity.NewContactForm" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>